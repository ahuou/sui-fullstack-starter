import type { SignatureScheme } from '.pnpm/@mysten+sui.js@0.54.1_typescript@5.8.3/node_modules/@mysten/sui.js/cryptography';
import { Signer } from '.pnpm/@mysten+sui.js@0.54.1_typescript@5.8.3/node_modules/@mysten/sui.js/cryptography';
import type { MultiSigPublicKey } from './publickey.js';
export declare class MultiSigSigner extends Signer {
    #private;
    constructor(pubkey: MultiSigPublicKey, signers?: Signer[]);
    getKeyScheme(): SignatureScheme;
    getPublicKey(): MultiSigPublicKey;
    sign(_data: Uint8Array): never;
    signData(_data: Uint8Array): never;
    signTransactionBlock(bytes: Uint8Array): Promise<{
        signature: string;
        bytes: string;
    }>;
    signPersonalMessage(bytes: Uint8Array): Promise<{
        signature: string;
        bytes: string;
    }>;
}
